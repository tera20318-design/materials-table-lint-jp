name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package and dev dependencies
        run: python -m pip install -e ".[dev,xlsx]"
      - name: Format check
        run: ruff format --check .
      - name: Lint
        run: ruff check .
      - name: Typecheck
        run: mypy src tests
      - name: Test
        run: pytest
      - name: CLI smoke
        run: |
          mtlint --version
          mtlint init --template heat-treatment --out heat_treatment.generated.schema.json
          mtlint init --template tensile-test --out tensile_test.generated.schema.json
          mtlint lint examples/clean.csv --schema examples/basic.schema.json
          mtlint lint examples/heat_treatment.csv --schema examples/heat_treatment.schema.json
          mtlint lint examples/tensile_test.csv --schema examples/tensile_test.schema.json
          mtlint normalize examples/clean.csv --schema examples/basic.schema.json --out normalized.csv --report report.json
      - name: Build
        run: python -m build
      - name: Check package metadata
        run: python -m twine check dist/*
